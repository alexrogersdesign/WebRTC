{
  "name": "@webrtc/monorepo",
  "private": true,
  "version": "0.0.1",
  "engines": {
    "node": "16.x",
    "npm": "6.x"
  },
  "workspaces": [
    "./backend/*",
    "./frontend/*",
    "./tests/*"
  ],
  "scripts": {
    "heroku-postbuild": "cd frontend && npm install && tsc -tsc --project tsconfig.json && npm run build",
    "start": "cd backend && npm start"
  },
  "devDependencies": {
    "concurrently": "^6.4.0",
    "typescript": "^4.5.2"
  }
}
